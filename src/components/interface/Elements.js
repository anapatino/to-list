export const headboard = (name) => {
  return `<header id="${name}" class="${name}" ></header>`;
};

export const main = (name) => {
  return `<main id="${name}" class="${name}" ></main>`;
};

export const footer = (name) => {
  return `<footer id="${name}" class="${name}" ></footer>`;
};
  
  